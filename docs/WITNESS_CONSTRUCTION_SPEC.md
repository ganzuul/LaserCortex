[{'cortex_name': 'string', 'form_type': 'threshold_category', 'form_schema_version': '0.1.0', 'coupling_signature': 'commutative" | "non_commutative" | "non_associative', 'axes': [], 'tensor_shape': [], 'validation': {'uncertainty': {'present': True, 'score': '0.0-1.0'}, 'witness': {'type': 'integer" | "string" | "boolean" | "number'}, 'binary_outcome': 'boolean', 'category_label': 'string'}, 'default_payload': {'magnitude_contract': {'witness_mass': '0.0-1.0', 'skeptic_mass': '0.0-1.0', 'balance_threshold': 0.05}, 'provenance': {'t': 'EMLTree) where\n  -- Cortex identification\n  cortexName       : String          --', 'tamari_classical': 'ormType         : String          --', 'tamari_contraction': 'ormSchemaVersion : String         --', '\n  \n  -- Coupling signature = rotation pattern\n  couplingSignature : CouplingSignature  -- Commutative | NonCommutative | NonAssociative\n  \n  -- Validation = Witness structure\n  steps            : List (EMLTree × EMLTree × ContractsOne)  -- Full step history\n  logicTypes       : List LogicType                           -- Logic type at each step\n  pentagonatorDistance : PentagonatorDistance                -- Magnitude contract\n  reflexiveDependencies : List (LogicType × LogicType)       -- Hot potato tracking\n  \n  -- Proof = Category valid repair (Law 2: monotonic provenance)\n  source           : s\n  target           : t\n  proof            : contracts_to s t                         -- Underlying contraction\n  \n  -- Provenance\n  provenance       : Provenance\n```\n\n### Coupling Signature Taxonomy (from document)\n> "The coupling signature must be assessed for appropriateness, with commutative signatures reserved for simple threshold categories and non-commutative or non-associative signatures applied to temporally ordered or self-referential cases."\n\n| Tamari Rotation Pattern | Coupling Signature | AssociahedronFace |\n|------------------------|-------------------|-------------------|\n| Single rotation (associativity) | Commutative | FuzzyBoundary |\n| Pentagonator (5 rotations) | NonCommutative | ParaconsistentBoundary |\n| Nested rotations (reflexive) | NonAssociative | QuantumBoundary |\n\n---\n\n## 4. Witness Extraction Specification for `contracts_to_trans`\n\n### Phase 5 Requirement (from document)\n> "The extraction hint must describe a reliably implementable computation from typical inputs... The mapping hint then specifies how the extracted witness maps to binary_outcome semantics.': 'Our `contracts_to_trans` Witness Extraction\n\nGiven:\n- `h₁ : contracts_to s t` (with witness `p₁ : ContractPath s t`)\n- `h₂ : contracts_to t u` (with witness `p₂ : ContractPath t u`)\n\n**Must produce**: `ContractPath s u` with:\n\n| Field | Extraction Computation | Mapping Hint |\n|-------|------------------------|--------------|\n| `steps` | `p₁.steps ++ p₂.steps` | Concatenate step histories |\n| `logicTypes` | `p₁.logicTypes ++ p₂.logicTypes` | Preserve logic-type sequence |\n| `pentagonatorDistance` | `composeDistance p₁.pentagonatorDistance p₂.pentagonatorDistance` | Sum values', 'd₂': 'PentagonatorDistance) : PentagonatorDistance :=\n  { value := d₁.value + d₂.value', 'faces': 'd₁.faces ++ d₂.faces', 'coherence': 'min d₁.coherence d₂.coherence'}, 'p₁': 'ContractPath s t) (p₂ : ContractPath t u) \n    : List (LogicType × LogicType) :=\n  (p₁.logicTypes ++ p₂.logicTypes).toPairs.filter (fun (a', 'The validation object defines the contract that any instantiated form_payload must satisfy... For threshold_category specs, this contract mandates four fields: uncertainty, witness, binary_outcome, and category_label.': 'ContractPath Validation (Lean 4)\n\n```lean\nstructure ContractPathValid where\n  -- Uncertainty = Pentagonator coherence check\n  uncertainty : ContractPathUncertainty\n  \n  -- Witness = Full topology present\n  witness : ContractPathWitness\n  \n  -- Binary outcome = Convergence achieved\n  binaryOutcome : Bool\n  \n  -- Category label = Normal form reached\n  categoryLabel : String\n\nstructure ContractPathUncertainty where\n  present : Bool := true\n  score   : ℚ    -- 1 - pentagonatorDistance.coherence\n\nstructure ContractPathWitness where\n  stepsPresent : Bool := true\n  topologyComplete : Bool  -- faces.length = value\n  reflexiveTracked : Bool  -- reflexiveDependencies detected\n```\n\n### Validation Function\n\n```lean\ndef validateContractPath (p : ContractPath s t) : ContractPathValid :=\n  { uncertainty := { present := true', 'score': 1, 'witness': {'stepsPresent': 'p.steps.length > 0', 'topologyComplete': 'p.pentagonatorDistance.faces.length = p.pentagonatorDistance.value', 'reflexiveTracked': True}, 'binaryOutcome': 'p.target = rightComb (size p.source))  -- Converged to right-comb?', 'categoryLabel': 'if p.target = rightComb (size p.source) then'}, 'Contract': 'The `contracts_to_trans` Theorem\n\n### Phase 5 Magnitude Contract (from document)\n```json', 'magnitude_contract': {'witness_mass': '0.0-1.0', 'skeptic_mass': '0.0-1.0', 'balance_threshold': 0.05, 'required': True, 'min': 0.0, 'max': 1.0}, 'u': 'EMLTree'}, 'h₁ : contracts_to s t) (h₂ : contracts_to t u) : \n    ContractPath s u := by\n  have h₁_witness : ContractPath s t := get_witness h₁\n  have h₂_witness : ContractPath t u := get_witness h₂\n  \n  -- Compose witnesses (the hot potato pass)\n  let composed : ContractPath s u := {\n    cortexName :=', 'tamari_classical', 'formType :=', 'tamari_contraction', 'formSchemaVersion :=', '0.1.0', '\n    couplingSignature := inferCoupling h₁_witness h₂_witness\n    steps := h₁_witness.steps ++ h₂_witness.steps\n    logicTypes := h₁_witness.logicTypes ++ h₂_witness.logicTypes\n    pentagonatorDistance := composeDistance h₁_witness.pentagonatorDistance h₂_witness.pentagonatorDistance\n    reflexiveDependencies := detectReflexiveLoops h₁_witness h₂_witness\n    source := s\n    target := u\n    proof := contracts_to.trans h₁ h₂\n    provenance := composeProvenance h₁_witness.provenance h₂_witness.provenance\n  }\n  \n  -- Validate magnitude contract (balance_threshold = 0.05)\n  have h_balance : composed.pentagonatorDistance.coherence ≥ 0.05 := by sorry\n  \n  exact composed\n```\n\n### Balance Threshold Enforcement\n\n> "The default balance_threshold of 0.05 provides a narrow tolerance that prevents premature termination while avoiding excessive iteration.', 'lean\ndef coherenceThreshold : ℚ := 0.05  -- 5% minimum coherence\n\ntheorem contracts_to_trans_preserves_coherence {s t u : EMLTree', 'h₁ : contracts_to s t) (h₂ : contracts_to t u) :\n    (contracts_to_trans h₁ h₂).pentagonatorDistance.coherence ≥ coherenceThreshold := by sorry\n```\n\n---\n\n## 7. Provenance Architecture\n\n### Phase 5 Provenance (from document)\n>', 'Every spec carries a provenance object containing the original prompt', 'agent version identifier', 'human review status', 'reviewer identity', 'creation timestamp', 'and explicit human notes guiding the review process.', 'ContractPath Provenance\n\n```lean\nstructure Provenance where\n  originalPrompt      : String  --', 'contracts_to_trans composition', 'agentVersion        : String  --', 'lean4_tamari_v0.1', 'humanReviewed       : Bool    -- false (automated)\n  reviewerId          : Option String\n  creationTimestamp   : String  -- ISO 8601\n  humanNotes          : String  --', 'Auto-generated from transitivity proof', 'seedSource          : String  --', 'EMLRegistry.lean:contracts_to_trans', 'validationStatus    : ValidationStatus\n  reuseCount          : ℕ       -- 0 initially\n```\n\n### Validation Status\n\n```lean\ninductive ValidationStatus where\n  | PendingReview      -- Awaiting human review\n  | Validated          -- Passed automated validation\n  | Rejected String    -- Failed with reason\n  | Recalibrated       -- Uncertainty score adjusted\n```\n\n---\n\n## 8. Registry Integration: Lookup and Instantiate\n\n### Phase 5 Reuse Path (from document)\n>', 'When a new inference arrives with a function concept and working interpretation', 'the registry computes a match key and queries for an existing cortex_id... instantiate constructs a fully typed Concept with a completed form_payload that will pass validate_typed_form without bootstrap traversal.', 'Tamari Registry Interface\n\n```lean\nstructure TamariRegistry where\n  -- Store ContractPath specifications by context\n  insert : ContextKey → ContractPathSpec → IO Unit\n  \n  -- Lookup by function concept + working interpretation\n  lookup : ContextKey → IO (Option ContractPathSpec)\n  \n  -- Instantiate with concrete trees\n  instantiate : ContractPathSpec → EMLTree → EMLTree → IO (ContractPath s t)\n  \n  -- Validate stored spec\n  validate : ContractPathSpec → IO ValidationResult\n```\n\n### Context Key for Tamari\n\n```lean\nstructure ContextKey where\n  functionConcept : String  --', 'contracts_to', 'workingInterpretation : String  --', 'tamari_contraction_to_right_comb', 'logicType : LogicType   -- Classical | Fuzzy | Paraconsistent | Quantum | ...\n  treeSize : ℕ            -- Number of leaves\n```\n\n---\n\n## 9. Human Review Rubric (Adapted from Phase 5)\n\n### Phase 5 Review Dimensions (from document)\n1. **Witness extraction computability** → Our `composeDistance` and `detectReflexiveLoops` must be computable\n2. **Default payload uncertainty conservatism** → Initial `coherence = 1.0` for Classical', 'lower for boundaries\n3. **Mapping hint correctness** → `binaryOutcome` = convergence to right-comb\n4. **Coupling signature appropriateness** → NonAssociative for reflexive loops\n5. **Examples fidelity** → Test cases in Lean 4\n\n### Tamari Review Checklist\n\n| Dimension | Tamari Check |\n|-----------|--------------|\n| Witness computable | `composeDistance` and `detectReflexiveLoops` are pure Lean functions |\n| Uncertainty conservative | Classical starts at 1.0; boundaries at 0.0; reflexive loops force ≤ 0.5 |\n| Binary outcome correct | `target = rightComb (size source)` |\n| Coupling correct | `inferCoupling` returns NonAssociative when reflexiveDependencies nonempty |\n| Examples verified | Unit tests for n=3', '4,5', 'pentagonator cases |\n\n---\n\n## 10. Implementation Plan: From Spec to Lean 4 Code\n\n### Phase 0: Core Types (Week 0) ✅ **COMPLETED in this document**\n- [x] `ContractPath` structure with all fields\n- [x] `PentagonatorDistance` with coherence semantics\n- [x] `AssociahedronFace` inductive (11 boundaries)\n- [x] `CouplingSignature` taxonomy\n- [x] `Provenance` structure\n- [x] `Validation` structures\n\n### Phase 1: Witness Construction (Week 1)\n- [ ] Implement `get_witness : contracts_to s t → ContractPath s t`\n- [ ] Implement `composeDistance`\n- [ ] Implement `detectReflexiveLoops`\n- [ ] Implement `inferCoupling`\n- [ ] Prove `contracts_to_trans` with full witness\n\n### Phase 2: Validation & Registry (Week 2)\n- [ ] Implement `validateContractPath`\n- [ ] Implement `TamariRegistry` interface\n- [ ] Add `ContextKey` matching\n- [ ] Integration test: compose 3+ paths', 'verify no reflexive loops\n\n### Phase 3: Human Review Gate (Week 3)\n- [ ] Export `ContractPath` to JSON (Phase 5 schema)\n- [ ] Import reviewed specs back to Lean\n- [ ] Automated validation pipeline\n- [ ] Metrics: reuse rate', 'uncertainty reduction\n\n---\n\n## 11. Critical Insight: The Curry-Howard Correspondence\n\n### Phase 5 Statement (from document)\n>', 'The Curry-Howard inspired distinction—where a proof of disjunction carries a witness of the disjunct taken—applied to moderation and threshold decisions', 'is absent from the NormCode design.', 'Tamari Realization\n\nOur `ContractPath` **is** the Curry-Howard witness:\n- `contracts_to s t` = proposition', 's contracts to t', 'ContractPath s t` = proof term carrying:\n  - **Which disjunct** = `logicTypes` (which logic-type at each step)\n  - **Which path** = `steps` (exact rotation sequence)\n  - **Topology** = `pentagonatorDistance.faces` (associahedron faces)\n  - **Coherence** = `pentagonatorDistance.coherence` (witness mass)\n\nThe **router** is the **skeptic** that must match this topology or carry the hot potato (reflexive dependency).\n\n---\n\n## 12. Appendix: Complete Lean 4 Type Definitions\n\n```lean\n-- In LaserCortex/EMLRegistry.lean\n\ninductive AssociahedronFace where\n  | FuzzyBoundary\n  | ParaconsistentBoundary\n  | QuantumBoundary\n  | TemporalBoundary\n  | DeonticBoundary\n  | EpistemicBoundary\n  | RelevanceBoundary\n  | FreeBoundary\n  | InfinitaryBoundary\n  | ModalBoundary\n  | IntuitionisticBoundary\n\nstructure PentagonatorDistance where\n  value : ℕ\n  faces : List AssociahedronFace\n  coherence : ℚ\n\ninductive CouplingSignature where\n  | Commutative\n  | NonCommutative\n  | NonAssociative\n\ninductive LogicType where\n  | Classical\n  | Fuzzy\n  | Paraconsistent\n  | Quantum\n  | Temporal\n  | Deontic\n  | Epistemic\n  | Relevance\n  | Free\n  | Infinitary\n  | Modal\n  | Intuitionistic\n\nstructure ContractPath (s t : EMLTree) where\n  cortexName : String\n  formType : String\n  formSchemaVersion : String\n  couplingSignature : CouplingSignature\n  steps : List (EMLTree × EMLTree × ContractsOne)\n  logicTypes : List LogicType\n  pentagonatorDistance : PentagonatorDistance\n  reflexiveDependencies : List (LogicType × LogicType)\n  source : s\n  target : t\n  proof : contracts_to s t\n  provenance : Provenance\n\nstructure Provenance where\n  originalPrompt : String\n  agentVersion : String\n  humanReviewed : Bool\n  reviewerId : Option String\n  creationTimestamp : String\n  humanNotes : String\n  seedSource : String\n  validationStatus : ValidationStatus\n  reuseCount : ℕ\n\ninductive ValidationStatus where\n  | PendingReview\n  | Validated\n  | Rejected (reason : String)\n  | Recalibrated\n\n-- THE KEY THEOREM\ntheorem contracts_to_trans {s t u : EMLTree', 'h₁ : contracts_to s t) (h₂ : contracts_to t u) : \n    ContractPath s u := by sorry\n```\n\n---\n\n## Document Control\n\n| Version | Date | Author | Changes |\n|---------|------|--------|---------|\n| 1.0 | 2026-06-07 | System | Extracted from Phase 5 architecture; mapped to Tamari/Logic of Will |\n\n**Status**: Specification Complete  \n**Next**: Implement Phase 1 witness construction in Lean 4']