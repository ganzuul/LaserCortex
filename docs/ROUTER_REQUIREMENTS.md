["Classical boundary] → [Fuzzy boundary] → [Paraconsistent boundary] \n  → [Quantum boundary] → Right-Comb\n```\n\nEach boundary crossing = **logic-type transition** = choice of which logic applies to which paradox.\n\n**Critical insight**: The convergence path itself **contains the history of logic-type choices**. The router must preserve this history for audit-trail integrity.\n\n---\n\n## 2. Pentagonator and Associahedron Distance\n\n### The Associahedron as Dependency Graph\n\n| Graph Element | Meaning in Logic-of-Will |\n|---------------|--------------------------|\n| Vertex        | Tree configuration (choice history) |\n| Edge          | Single choice (rotation) |\n| Face          | Logic-type boundary |\n| 3D Cell (Pentagonator) | Logic-type transition coherence |\n\n### Pentagonator Distance Metric\n\n```lean\nstructure PentagonatorDistance where\n  value : ℕ  -- Number of nested logic-type transitions\n  faces : List AssociahedronFace  -- Sequence of faces crossed\n  coherence : ℚ  -- 0.0 = boundary, 1.0 = center\n```\n\n| Distance | Meaning |\n|----------|---------|\n| 0        | On logic-type boundary (ready to transition) |\n| 1        | One logic-type transition away |\n| k        | k nested logic-type transitions needed |\n\n---\n\n## 3. The Reflexive Implication Problem\n\n### What Happens Without Tracking\n\nIf the router doesn't track pentagonator distance:\n\n1. **Circular Dependencies (Cortical Loop)**\n   ```\n   Classical → Fuzzy → Paraconsistent → Classical\n   ```\n   The router keeps cycling, never converging.\n\n2. **Unnecessary Transitions**\n   The router makes redundant logic-type transitions, wasting compute.\n\n3. **Missed Dependencies**\n   The audit trail (`contracts_to`) loses track of which logic-type was active at each step, breaking monotonic provenance (Law 2).\n\n### The Reflexive Loop\n\n```\nConvergence Path → Logic-Type History → Router Decision → New Path\n     ↑                                                              |\n     └────────────────── Reflexive Loop ────────────────────────────┘\n```\n\nThe router's decision affects the next convergence path, creating a **reflexive loop**.\n\n---\n\n## 4. Router Requirements Specification\n\n### Core Types\n\n```lean\n-- A witnessed contraction path with full audit trail\nstructure ContractPath (s t : EMLTree) where\n  steps : List (EMLTree × EMLTree × ContractsOne)  -- Full step history\n  logicTypes : List LogicType                      -- Logic type at each step\n  pentagonatorDistance : PentagonatorDistance     -- Distance to associahedron center\n  reflexiveDependencies : List (LogicType × LogicType)  -- Tracked reflexive loops\n  source : s\n  target : t\n  proof : contracts_to s t                          -- Underlying contraction proof\n\n-- Logic-type boundary faces on the associahedron\ninductive AssociahedronFace where\n  | FuzzyBoundary          -- Fuzzy ↔ ManyValued boundary\n  | ParaconsistentBoundary -- Classical ↔ Paraconsistent boundary\n  | QuantumBoundary        -- Classical ↔ Quantum boundary\n  | TemporalBoundary       -- Classical ↔ Temporal boundary\n  | DeonticBoundary        -- Classical ↔ Deontic boundary\n  | EpistemicBoundary      -- Classical ↔ Epistemic boundary\n  | RelevanceBoundary      -- Classical ↔ Relevance boundary\n  | FreeBoundary           -- Classical ↔ Free boundary\n  | InfinitaryBoundary     -- Classical ↔ Infinitary boundary\n  | ModalBoundary          -- Classical ↔ Modal boundary\n  | IntuitionisticBoundary -- Classical ↔ Intuitionistic boundary\n\n-- Distance to associahedron center (pentagonator)\nstructure PentagonatorDistance where\n  value : ℕ\n  faces : List AssociahedronFace\n  coherence : ℚ  -- 0.0 = boundary, 1.0 = center\n\n-- Router contract between two cortical interfaces\nstructure RouterContract (C₁ C₂ : CorticalInterface) where\n  translate : C₁.EMLTree → C₂.EMLTree\n  crossContract : C₁.contracts_to → C₂.contracts_to\n  faceOf : C₁.EMLTree → Option AssociahedronFace\n  boundaryDistance : C₁.EMLTree → PentagonatorDistance\n  reflexiveDependencies : C₁.EMLTree → List (LogicType × LogicType)\n  coherence : ∀ (t : C₁.EMLTree), contracts_to_trans_preserves_coherence t\n```\n\n### Critical: `contracts_to_trans` Must Have Full Witness\n\nThe current `sorry`'d `contracts_to_trans` **must** produce a `ContractPath` with full witness:\n\n```lean\ntheorem contracts_to_trans {s t u : EMLTree} \n    (h₁ : contracts_to s t) (h₂ : contracts_to t u) : \n    ContractPath s u := by\n  -- Must construct full witness:\n  -- 1. Compose steps from h₁ and h₂\n  -- 2. Compose logicTypes\n  -- 3. Sum pentagonatorDistances\n  -- 4. Detect new reflexive dependencies\n  -- 5. Produce combined ContractPath with proof = h₁.trans h₂\n  sorry\n```\n\n---\n\n## 5. Cortical Interface Requirements\n\n### Each logic type implements `CorticalInterface`\n\n```lean\nstructure CorticalInterface where\n  EMLTree : Type\n  contracts_one : EMLTree → EMLTree → Prop\n  contracts_to : EMLTree → EMLTree → Prop\n  rightComb : ℕ → EMLTree\n  LogicType : Type\n  normalForm : EMLTree → EMLTree\n  rotation : ∀ (a b c : EMLTree), contracts_one (Node (Node a b) c) (Node a (Node b c))\n  \n  -- Router-facing methods\n  faceOf : EMLTree → Option AssociahedronFace\n  boundaryDistance : EMLTree → PentagonatorDistance\n  reflexiveDependencies : EMLTree → List (LogicType × LogicType)\n  translateTo : ∀ (other : CorticalInterface), EMLTree → other.EMLTree\n```\n\n### Classical/Tamari Implementation (Priority 0)\n\n```lean\ndef ClassicalCortical : CorticalInterface := {\n  EMLTree := EMLTree\n  contracts_one := contracts_one\n  contracts_to := contracts_to\n  rightComb := rightComb\n  LogicType := Unit\n  normalForm := rightComb\n  rotation := fun a b c => .rotate a b c\n  faceOf := fun _ => none  -- Classical is the center, no boundaries\n  boundaryDistance := fun _ => { value := 0, faces := [], coherence := 1.0 }\n  reflexiveDependencies := fun _ => []\n  translateTo := fun other t => other.translateFromClassical t\n}\n```\n\n---\n\n## 5. Implementation Phases\n\n### Phase 0: Core Witness Types (Immediate)\n- [ ] Define `ContractPath` with full witness\n- [ ] Define `AssociahedronFace` and `PentagonatorDistance`\n- [ ] Implement `contracts_to_trans` with full witness\n\n### Phase 1: Cortical Interface (Week 1)\n- [ ] Implement `CorticalInterface` for Classical/Tamari\n- [ ] Implement `RouterContract` for Classical ↔ other logics\n- [ ] Add `faceOf` and `boundaryDistance` for Tamari lattice\n\n### Phase 2: Cross-Logic Router (Week 2)\n- [ ] Implement `translate` between logic types\n- [ ] Implement `crossContract` composition\n- [ ] Add reflexive dependency detection\n\n### Phase 3: Reflexive Loop Detection (Week 3)\n- [ ] Detect circular logic-type dependencies\n- [ ] Implement namespace freeze for trivialization cascades\n- [ ] Add human review triggers\n\n---\n\n## 5. Mathematical Justification\n\n### Why Pentagonator Distance Matters\n\nThe **pentagonator** represents the **coherence of associativity** at the 4th level. In the logic-of-will interpretation:\n\n| Distance | Meaning |\n|----------|---------|\n| 0        | On logic-type boundary (ready to transition) |\n| 1        | One logic-type transition away |\n| k        | k nested logic-type transitions needed |\n\nThe **reflexive implication** of convergence means: when a tree contracts to its normal form, it may pass through **multiple logic-type boundaries**. The router must track this to avoid:\n- **Circular dependencies** (cortical loop)\n- **Unnecessary transitions** (wasted compute)\n- **Missed dependencies** (incomplete audit trail)\n\n### The Associahedron as Dependency Graph\n\n| Graph Element | Meaning |\n|---------------|---------|\n| Vertex        | Tree configuration (choice history) |\n| Edge          | Single choice (rotation) |\n| Face          | Logic-type boundary |\n| 3D Cell (Pentagonator) | Logic-type transition coherence |\n\nThe router **is a walker on this graph** that must:\n1. Know its current face\n2. Track distance to pentagonator\n3. Compose paths correctly\n4. Minimize pentagonator crossings\n\n---\n\n## 6. Summary of Requirements\n\n| Requirement | Current Status | Must Implement |\n|-------------|----------------|----------------|\n| `ContractPath` witness | ❌ Missing | ✅ Required |\n| `AssociahedronFace` type | ❌ Missing | ✅ Required |\n| `PentagonatorDistance` | ❌ Missing | ✅ Required |\n| `contracts_to_trans` with witness | ❌ `sorry` | ✅ Required |\n| `CorticalInterface` | ❌ Missing | ✅ Required |\n| `RouterContract` | ❌ Missing | ✅ Required |\n| Reflexive dependency tracking | ❌ Missing | ✅ Required |\n\n---\n\n## Document Control\n\n| Version | Date | Author | Changes |\n|---------|------|--------|---------|\n| 1.0 | 2026-06-07 | System | Initial creation: router requirements from reflexive implication analysis |\n\n**Status**: Requirements Specification Complete  \n**Next**: Implement `ContractPath` and `contracts_to_trans` with full witness"]